import{_ as s,o as a,c as i,a2 as t}from"./chunks/framework.D8jXWrC0.js";const k=JSON.parse('{"title":"User identity","description":"","frontmatter":{},"headers":[],"relativePath":"security/walkthrough-user-identity.md","filePath":"security/walkthrough-user-identity.md"}'),n={name:"security/walkthrough-user-identity.md"};function r(o,e,c,h,d,l){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="user-identity" tabindex="-1">User identity <a class="header-anchor" href="#user-identity" aria-label="Permalink to &quot;User identity&quot;">​</a></h1><p>The user identity plane carries the human subject independently of the agent actor. Keycloak is the current user OIDC provider.</p><h2 id="gateway-verification" tabindex="-1">Gateway verification <a class="header-anchor" href="#gateway-verification" aria-label="Permalink to &quot;Gateway verification&quot;">​</a></h2><p>When <code>security.userAuth.issuer</code> is configured, <code>security.strictGatewayApi.enabled=true</code> is required so direct workload routing cannot bypass the gateway. Strict mode includes gateway-routed internal URLs and NetworkPolicy isolation. The operator adds a <code>user</code> JWT provider to each protected gateway <code>SecurityPolicy</code>. The provider reads the standard header:</p><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">Authorization</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> Bearer &lt;keycloak-user-jwt&gt;</span></span></code></pre></div><p>The gateway verifies the token against Keycloak through <code>remoteJWKS</code> and checks the configured issuer and optional audience. The PDP is authoritative for authorization and verifies the subject token again. The user provider is independent of <code>security.agentAuth.identity.provider</code>; Keycloak user tokens can accompany any supported agent issuer.</p><h2 id="keycloak-groups-claim-requirement" tabindex="-1">Keycloak groups claim requirement <a class="header-anchor" href="#keycloak-groups-claim-requirement" aria-label="Permalink to &quot;Keycloak groups claim requirement&quot;">​</a></h2><p>Group-based <code>AccessGrant</code>s require Keycloak access tokens to contain a <code>groups</code> claim. Configure a Group Membership protocol mapper with claim name <code>groups</code>, access-token claims enabled, and <code>full.path</code> set to <code>false</code>. KAOS therefore uses short group names: a Keycloak group named <code>researchers</code> appears as <code>&quot;researchers&quot;</code> in the claim and must be written as <code>name: researchers</code> in the <code>AccessGrant</code>.</p><p>This mapper is a hard requirement: without it, group-based grants cannot match. The CLI configures it automatically for the managed Keycloak preset. Bring-your-own-Keycloak deployments must configure the mapper themselves.</p><h2 id="accessgrant" tabindex="-1">AccessGrant <a class="header-anchor" href="#accessgrant" aria-label="Permalink to &quot;AccessGrant&quot;">​</a></h2><p><code>AccessGrant</code> is a namespaced CRD that grants users or groups entry to selected resources. A <code>User</code> subject matches the token&#39;s <code>sub</code> or <code>email</code>; a <code>Group</code> subject matches a short name in the <code>groups</code> claim. Resources can name an explicit kind and name or select resources by label.</p><p>For example, this grant lets members of the Keycloak <code>researchers</code> group enter the <code>researcher</code> Agent in the same namespace:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">apiVersion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">kaos.tools/v1alpha1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">AccessGrant</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">metadata</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">researchers-enter-researcher</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  namespace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">demo</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">spec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  subjects</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Group</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">researchers</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  resources</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Agent</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">researcher</span></span></code></pre></div><p>The operator compiles this grant to <code>data.kaos.user_grants</code>, keyed as <code>group:researchers</code>. User entries are keyed as <code>user:&lt;sub-or-email&gt;</code>.</p><p>Check whether the grant is enforced with:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">kubectl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> accessgrant</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> researchers-enter-researcher</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> demo</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -o</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> jsonpath=&#39;{.status.conditions[?(@.type==&quot;Enforced&quot;)]}&#39;</span></span></code></pre></div><p>Without a configured user identity provider, the object is accepted but its condition is <code>Enforced=False</code> with reason <code>NoUserIdentityProvider</code>, and it is not projected into policy data. Manual or disabled policy projection reports <code>PolicyProjectionInactive</code>, and a failed publication reports <code>ProjectionFailed</code>. The condition becomes <code>Enforced=True</code> only after automated policy projection successfully publishes the grant.</p><h2 id="user-entry" tabindex="-1">User entry <a class="header-anchor" href="#user-entry" aria-label="Permalink to &quot;User entry&quot;">​</a></h2><p>A user reaches a protected Agent by sending a Keycloak access token in <code>Authorization</code> to the Agent&#39;s external gateway route. The request has no agent actor token at this entry edge. The PDP verifies the user subject and allows the request only when an enforced <code>AccessGrant</code> for the user&#39;s <code>sub</code>, <code>email</code>, or one of their <code>groups</code> covers the path-derived target Agent. The Agent then supplies its own actor token on internal calls while propagating the original user token as the required subject.</p><h2 id="subject-propagation" tabindex="-1">Subject propagation <a class="header-anchor" href="#subject-propagation" aria-label="Permalink to &quot;Subject propagation&quot;">​</a></h2><p>The agent runtime captures the inbound <code>Authorization</code> bearer token in request-local context and propagates it unchanged on outbound agent calls. The runtime also carries the principal and correlation context. On each hop:</p><ul><li><code>Authorization</code> continues to represent the original user subject.</li><li><code>x-agent-authorization</code> represents the agent making that hop.</li></ul><p>Header injection is additive. An explicitly set outbound <code>Authorization</code> header, such as a ModelAPI provider credential, is not overwritten.</p>`,23)])])}const u=s(n,[["render",r]]);export{k as __pageData,u as default};

---
phase: 01-native-tool-calling
plan: 02
subsystem: api
tags: [crd, kubebuilder, go, python, pydantic, env-var, function-calling]

# Dependency graph
requires: []
provides:
  - "FunctionCalling field on AgentConfig CRD (native/text enum)"
  - "FUNCTION_CALLING env var passed from operator to agent pods"
  - "function_calling field on AgentServerSettings (Python)"
affects: [01-03, 01-04, 01-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "CRD field → env var → pydantic settings propagation pattern"

key-files:
  created: []
  modified:
    - "operator/api/v1alpha1/agent_types.go"
    - "operator/controllers/agent_controller.go"
    - "data-plane/kaos-framework/agent/server.py"
    - "operator/config/crd/bases/kaos.tools_agents.yaml"
    - "operator/chart/crds/agent-crd.yaml"

key-decisions:
  - "FunctionCalling field placed inside AgentConfig (not AgentSpec) for consistency with other config fields"
  - "Default handled both in kubebuilder marker (+kubebuilder:default=native) and Go code (empty string fallback)"

patterns-established:
  - "CRD-to-pod propagation: kubebuilder field → constructEnvVars mapping → pydantic BaseSettings auto-read"

# Metrics
duration: 4min
completed: 2026-02-14
---

# Phase 1 Plan 2: FunctionCalling Config Field Summary

**Added functionCalling CRD field (native/text enum) with full stack propagation: Go types → operator env var mapping → Python server settings**

## Performance

- **Duration:** 4 min
- **Started:** 2026-02-14T12:40:51Z
- **Completed:** 2026-02-14T12:45:04Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments
- AgentConfig CRD has FunctionCalling field with kubebuilder Enum validation (native/text)
- Operator constructEnvVars maps FunctionCalling to FUNCTION_CALLING env var with native default
- Python AgentServerSettings reads FUNCTION_CALLING env var via pydantic BaseSettings
- CRD manifests and Helm chart regenerated with the new field

## Task Commits

Each task was committed atomically:

1. **Task 1: Add FunctionCalling field to AgentConfig and regenerate** - `bf459bc` (feat)
2. **Task 2: Add FUNCTION_CALLING env var mapping in constructEnvVars** - `9f7efda` (feat)
3. **Task 3: Add function_calling to AgentServerSettings in server.py** - `587950a` (feat)

## Files Created/Modified
- `operator/api/v1alpha1/agent_types.go` - Added FunctionCalling field to AgentConfig struct
- `operator/controllers/agent_controller.go` - Added FUNCTION_CALLING env var mapping with native default
- `data-plane/kaos-framework/agent/server.py` - Added function_calling field to AgentServerSettings
- `operator/config/crd/bases/kaos.tools_agents.yaml` - Regenerated CRD with functionCalling field
- `operator/chart/crds/agent-crd.yaml` - Helm chart CRD updated
- `operator/chart/crds/mcpserver-crd.yaml` - Regenerated by helmify
- `operator/chart/crds/modelapi-crd.yaml` - Regenerated by helmify
- `operator/chart/values.yaml` - Regenerated by helmify

## Decisions Made
- FunctionCalling placed in AgentConfig (alongside Description, Instructions) not AgentSpec — follows existing pattern
- Default "native" enforced in both kubebuilder marker and Go runtime (belt-and-suspenders for empty omitempty)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- FunctionCalling field available for Plan 03 to wire into Agent constructor
- function_calling settings value ready for Agent class to consume
- All Go tests pass (28/28 integration tests), all Python tests pass (47/47)

## Self-Check: PASSED

All key files exist on disk. All 3 task commits verified in git history.

---
*Phase: 01-native-tool-calling*
*Completed: 2026-02-14*
